[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ping-monitor"
version = "0.1.0"
description = "URL ping monitoring service"
requires-python = ">=3.11"
dependencies = ["requests==2.31.0"]
